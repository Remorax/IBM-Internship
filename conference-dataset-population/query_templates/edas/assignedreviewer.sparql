PREFIX :<http://edas#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{
{{rev}} a :Reviewer.
	{{rev}} :isReviewing {{pap}}.
	{{review}} a :Review.
	{{rev}} :hasRelatedDocument {{review}}.
{{review}} rdfs:label {{reviewname}}.

{{rev}} :isMemberOf {{conf}}.
}
