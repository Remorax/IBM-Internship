PREFIX :<http://edas#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{
{{papv0}} :isWrittenBy {{auth}}.
{{pap}} :isWrittenBy {{auth}}.
{{auth}} a :Author.
{{auth}} :isMemberOf {{conf}}.
}
