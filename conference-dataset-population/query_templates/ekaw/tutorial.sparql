PREFIX :<http://ekaw#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{
{{tuto}} a :Tutorial.
	{{tuto}} rdfs:label {{tutoname}}.
	{{tuto}} :partOfEvent {{conf}}.	
	{{tutochair}} a :Tutorial_Chair.
	{{tutoabstr}} a :Tutorial_Abstract.
	{{tutoabstr}} rdfs:label {{tutoname}}.
	{{tutoabstr}} :paperPresentedAs {{tuto}}.
}
