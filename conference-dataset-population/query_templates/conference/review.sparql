PREFIX :<http://conference#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{

{{review}} a :Review.
	{{review}} :reviews {{pap}}.
	{{rev}} a :Reviewer.
	{{review}} :has_authors {{rev}}.
	{{review}} rdfs:label {{reviewname}}.

}
