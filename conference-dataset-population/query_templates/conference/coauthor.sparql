PREFIX :<http://conference#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{
{{pap}} :has_authors {{auth}}.
{{papabstr}} :has_authors {{auth}}.
{{auth}} a :Contribution_co-author.
}
