PREFIX :<http://cmt#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{
{{pap}} :readByMeta-Reviewer {{rev}}.
	{{rev}} a :Meta-Reviewer.
	{{rev}} :writeReview {{review}}.
	{{review}} a :Meta-Review.
{{review}} rdfs:label {{reviewname}}.
{{rev}} :memberOfConference {{conf}}.
}
