PREFIX :<http://cmt#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{

{{pap}} :readByReviewer {{rev}}.
	{{rev}} a :Reviewer.
	{{rev}} :writeReview {{review}}.
	{{review}} a :Review.
{{review}} rdfs:label {{reviewname}}.
{{rev}} :memberOfConference {{conf}}.

}
