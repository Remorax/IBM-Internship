PREFIX :<http://cmt#>
PREFIX rdfs:<http://www.w3.org/2000/01/rdf-schema#>

INSERT DATA{

{{admin}} :assignReviewer {{rev}}.
	{{rev}} a :Reviewer.
	{{rev}} :hasBeenAssigned {{pap}}.

{{rev}} :memberOfConference {{conf}}.
}
