PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
PREFIX out: <http://subgraph-alignment#>

INSERT DATA {
{{sourceSubgraph}} out:hasIntensionForm {{form}}.
{{sourceSubgraph}} out:hasExtensionForm {{form}}.
}